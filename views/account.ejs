<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Log In</title>
</head>
<body>
  <h1>Here are your account details <%= user.username %> id: <%= user.id %>!</h1>
  <h2>Your savings: $<%= (user.money / 100).toFixed(2) %></h2>
  <form action="/account/money?_method=PUT" method="POST">
    <label for="money">Amount</label>
    <input id="money" name="money" type="number" step = "0.01" required/>
    <button type="submit">Change Account Balance</button>
  </form>
  <h2>Your maximum credit: $<%= (user.credit / 100).toFixed(2) %></h2>
  <form action="/account/credit?_method=PUT" method="POST">
    <label for="credit">Amount</label>
    <input id="credit" name="credit" type="number" step = "0.01" required/>
    <button type="submit">Change Account Balance</button>
  </form>
  <h2>Pay off credit?</h2>
  <form action="/account/credit?_method=PUT" method="POST">
    <label for="credit">Amount</label>
    <input id="credit" name="credit" type="number" step = "0.01" required/>
    <button type="submit">Pay off Balance</button>
  </form>
<a href="/">Back</a><br>
<a href="/log-out">LOG OUT</a><br>
</body>
</html>
